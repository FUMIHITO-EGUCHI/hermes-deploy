{
  "name": "PROJECT_NAME_PLACEHOLDER",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "precommit:check": "echo '(precommit:check is a no-op until configured — replace this with npm run typecheck etc.)'"
  }
}
